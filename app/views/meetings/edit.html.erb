<h1>研究会の編集</h1>

<% form_for(@meeting) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => f %>
  <p>
    <%= f.submit '更新' %>
  </p>
<% end %>

<%= link_to '詳細', @meeting %> |
<%= link_to '戻る', meetings_path %>
